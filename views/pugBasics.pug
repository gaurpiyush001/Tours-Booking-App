//- Pug is a White Space Sensitive Syntax, for wrinting HTML
doctype html
html
 head 
  // Interpolation syntax, just to add something to string
  title Natours | #{tour}
  link(rel='stylesheet' href='css/style.css')
  link(rel='shortcut icon' type='image/png' href='img/favicon.png') 

 body
  // buffered code, which will be shown in output through a variable
  h1= tour
  h2= user.toUpperCase()
  // h1 The Park Camper, this will create a html comment, another way is //-
  
  //below is Unbuffered Code, not shown in output
  - const x = 9;
  h2= 2*x

  p This is some text
   


